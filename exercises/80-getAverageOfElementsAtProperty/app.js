// Write your function here


const getAverageOfElementsAtProperty = (array, key) => {
    if(!array.isarray(object[key])) && (key in object) && (object[key].length > 0)
    object[key].foreach(element) {

    }
    return 0; 
    
}

var obj = {
  key: [1, 2, 3]
};
var output = getAverageOfElementsAtProperty(obj, 'key');
console.log(output); // --> 2

